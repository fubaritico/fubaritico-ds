/** Default distance (px) between the target element and the tooltip bubble. */
export const DEFAULT_OFFSET = 10

/**
 * Width of the arrow triangle (px). Mirrors the `border-width` of `.ui-tooltip__arrow` in the skin —
 * keep the two in sync (the positioning math offsets the arrow by this value).
 */
export const ARROW_SIZE = 6
